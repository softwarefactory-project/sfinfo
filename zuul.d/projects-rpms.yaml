---
- project:
    name: ^rpms/.*$
    templates:
      - sf-ci-jobs-light

- project:
    name: ^rpms/zuul.*$
    templates:
      - sf-ci-jobs
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build

- project:
    name: rpms/nodepool
    templates:
      - sf-ci-jobs
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
