---
- project:
    name: ^rpms/.*$
    check:
      jobs:
        - noop
    gate:
      jobs:
        - sf-rpm-build
        - wait-for-changes-ahead:
            dependencies:
              - sf-rpm-build
        - sf-rpm-publish:
            dependencies:
              - wait-for-changes-ahead
